# Cambios, arreglos, mejoras
 - [Cambio] Plugin uicon
 - [Cambio] Ya se puede editar / ocultar comentario
 - [Cambio] Altura del modal ajustada
 - [Nuevo] Usar página modo caja o completa
 - [Nuevo] Plugin para google analytics (activar desde main_header.tpl)

### Ejecutar consulta
> No olvides reemplazar el @ por el prefijo escogido en la instalación (ver en .env 'ZCODE_DB_PREFIX')
``` sql
ALTER TABLE `@perfil` ADD `user_pagebox` INT NOT NULL DEFAULT '0' AFTER `user_font_size`;
ALTER TABLE `@seo` ADD `seo_google_verification` varchar(60) NOT NULL DEFAULT '' AFTER `seo_sitemap`, ADD `seo_google_verification_active` int(1) NULL DEFAULT 0 AFTER `seo_google_verification`;
```

 ---
 _Cambios totales: 20_