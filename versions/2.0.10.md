# Cambios, arreglos, mejoras
 - [Cambio] Plugin meta (añade a google verification y analytics)
 - [Cambio] Se movio los js al <head>

### Ejecutar consulta
> No olvides reemplazar el @ por el prefijo escogido en la instalación (ver en .env 'ZCODE_DB_PREFIX')
``` sql
ALTER TABLE `@seo` ADD `seo_google_analytics` VARCHAR(20) NOT NULL DEFAULT '' AFTER `seo_google_verification_active`;
```

 ---
 _Cambios totales: 9_