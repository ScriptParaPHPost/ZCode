> Solo si ya tienes el script instalado, si lo vas a instalar, no requires realizar los siguientes pasos

 * Smarty 4.5.4 => Smarty 4.5.5

# Pasos a realizar

> Claramente donde esta @ deben reemplazarlo por el prefix que hayan escogido, si por defecto entonces será zcode_

``` sql
ALTER TABLE `@configuracion` 
ADD `c_ver_vistas_global` int(1) NOT NULL DEFAULT 0, 
ADD `c_quitar_vistas_global` int(1) NOT NULL DEFAULT 0, 
ADD `c_visitas_tiempo` int(2) NOT NULL DEFAULT 5;
```

``` sql
CREATE TABLE IF NOT EXISTS `zc_conexion_actual` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `ip` varchar(50) NOT NULL DEFAULT '',
  `session_id` varchar(100) NOT NULL DEFAULT '',
  `last_activity` int(11) NOT NULL DEFAULT 0,
  PRIMARY KEY (`id`),
  UNIQUE KEY (ip, session_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

# FIX
 - .htaccess, no se podía leer los mensajes
 - No se podía enviar mensajes
 - Avatar lo guarda bien, pero no hacia uso del mismo