# CHANGELOG

# 🔨 Arreglos
 - Ver todas las categorías (admin)
 - Agregar/Eliminar/Activar afiliado (admin/home)

# 📣 Cambios
 - Se movió clase de categorías por separado
 - Se edito el afiliado.js

# 🐛 Errores
 - Puedes que no tengas la consulta realizada
 	```sql
 	ALTER TABLE zcode_posts_categorias ADD `c_private` int(1) NOT NULL DEFAULT 0;
 	```
 	cambia el `zcode_` por tu prefijo